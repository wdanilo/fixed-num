[package]
name = "fiexed-num-test-nightly"
version = { workspace = true }
edition = {workspace = true }

[dependencies]
fixed-num = { path = "../../crates/lib", features = ["serde", "serde_float"] }

[lints]
workspace = true
